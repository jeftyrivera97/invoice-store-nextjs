import { Button } from "@/components/ui/button";
import { RefreshCcw } from "lucide-react";

export const ComprobanteRefreshProductButton = () => {
  return (
    <Button variant="secondary" size="icon" className="size-8">
      <RefreshCcw />
    </Button>
  );
};
